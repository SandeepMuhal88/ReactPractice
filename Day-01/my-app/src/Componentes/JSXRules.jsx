import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>1. Use a single parent element</p>
        <p>2. Use a single parent element</p>
        <p>JSX attributes are written using camelCase (e.g., className instead of class)</p>
    </div>
  )
}

export default JSXRules